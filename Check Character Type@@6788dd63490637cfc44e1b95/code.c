#include <stdio.h>
int main() {
    char ch;
    ch1=toupper(ch)
    switch(){
        case "A":case "E":case "I":case "O":case "U"{
            printf("Vowel")
        }
        
    }
    printf("%s", welcome());
    return 0;
}